# Our how-to docs will use --debug parameter which is only available in Flask 2.2+
Flask>=2.2
# If Flask-Session is not maintained in future, Flask-Session2 should work as well
Flask-Session>=0.3.2,<0.5
werkzeug>=2
requests>=2,<3

#identity>=0.2,<0.3
git+https://github.com/AzureAD/microsoft-authentication-library-for-python.git@token-validation
git+https://github.com/rayluo/identity.git@decorator

python-dotenv<0.22
